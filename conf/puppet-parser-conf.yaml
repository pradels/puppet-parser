# Section ignore_paths lists all path names that should be ignored and should
# not be parsed.
ignore_paths:

  # List of paths that match if they begin with specified path.
  begin_with:
    - /srv/data/prod/sys-mgmt/puppet/local/environments/test/config
    - /srv/data/prod/sys-mgmt/puppet/local/environments/pred/config
    - /srv/data/prod/sys-mgmt/puppet/local/environments/prod/config

